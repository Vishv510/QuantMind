package com.stock.common.config;

import org.springframework.context.annotation.Configuration;

/**
 * Common configuration for all services
 * This class can be extended to add shared beans, filters, interceptors, etc.
 */
@Configuration
public class CommonConfig {

    // Common beans and configurations will be added here
    // Examples: RestTemplate, WebClient, ObjectMapper, etc.
}
